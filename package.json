{
  "name": "backend",
  "module": "src/index.ts",
  "devDependencies": {
    "@biomejs/biome": "^1.5.2",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "start": "bun src/index.ts",
    "dev": "bun --watch src/index.ts",
    "build": " bun build src --splitting --outdir=out",
    "format": "bunx biome format --write .",
    "lint": "bunx biome lint ."
  },
  "type": "module",
  "dependencies": {
    "@sequelize/core": "alpha",
    "chalk": "4.1.2",
    "pg": "^8.11.3",
    "valibot": "^0.27.0"
  }
}
